import * as numberUtils from './number.util';
import * as orderingUtils from './ordering.util';
import * as randomUtils from './random.util';

export const NumberUtils = { ...numberUtils };
export const OrderingUtils = { ...orderingUtils };
export const RandomUtils = { ...randomUtils };
